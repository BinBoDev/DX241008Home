<Window x:Class="DX.View.Admin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:DX.View"
        mc:Ignorable="d"
        Title="Admin" Height="450" Width="800"
        Icon="/DX;component/View/Image/Uni.ico"
        Loaded="Window_Loaded"
        >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <materialDesign:Card Margin="5" BorderThickness="0">
            <StackPanel Orientation="Horizontal">
                <Menu HorizontalAlignment="Center" >
                    <MenuItem Header="File" Height="20" Margin="2"  >
                        
                    </MenuItem>
                    <MenuItem Header="Edit" Height="20" Margin="2" >
                        
                    </MenuItem>
                </Menu>
            </StackPanel>
        </materialDesign:Card>
        <materialDesign:Card Grid.Row="1" Margin="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBox
                              
                              Text="{Binding SelectedAccount.Username,UpdateSourceTrigger=PropertyChanged}"
                              Name="txtUsername"  
                              Margin="5"
                              VerticalAlignment="Center"
                              materialDesign:HintAssist.Hint="Tên đăng nhập"
                              Style="{StaticResource MaterialDesignFloatingHintTextBox}" />
                        <TextBox
                              Text="{Binding SelectedAccount.Password,UpdateSourceTrigger=PropertyChanged}"
                              Name="txtPasword"   
                              Margin="5"
                              VerticalAlignment="Center"
                              materialDesign:HintAssist.Hint="Password"
                              Style="{StaticResource MaterialDesignFloatingHintTextBox}" /> 
                        <RadioButton 
                              Content="Admin" Margin="20,5,10,5" Name="rbAdmin" Tag="1"
                              IsChecked="{Binding SelectedAccount.Type}"
                              />
                        <RadioButton Content="Sub" Margin="5" Name="rbSub" Tag="2"
                                     IsChecked="{Binding SelectedAccount.Type}"
                                     />
                        <RadioButton 
                                     Content="Nomal" Name="rbNomal" Tag="3"
                                     IsChecked="{Binding SelectedAccount.Type}"
                                     />
                        <Button Content="Thêm" Margin="20,5,10,5" Name="btnAdd" Background="Green"/>
                    </StackPanel>

                    <DataGrid Grid.Row="1" CanUserAddRows="False" AutoGenerateColumns="False" ItemsSource="{Binding Accounts ,UpdateSourceTrigger=PropertyChanged}" SelectedItem="{Binding SelectedAccount}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Id" Binding="{Binding Id}"/>
                            <DataGridTextColumn Header="Username" Binding="{Binding Username}"/>
                            <DataGridTextColumn Header="Password" Binding="{Binding Password}"/>
                            <DataGridTextColumn Header="Type" Binding="{Binding Type}"/>
                            <DataGridTemplateColumn Header="Delete">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Name="btnDel" Background="Red" Command="{Binding DataContext.DelCommand, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}">
                                            <StackPanel Orientation="Horizontal">
                                                <materialDesign:PackIcon Kind="DeleteOutline"  />
                                                <TextBlock Text="Xóa"/>
                                            </StackPanel>
                                        </Button>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
                <Grid Grid.Row="1">
                    
                </Grid>
            </Grid>
        </materialDesign:Card>
    </Grid>
</Window>
